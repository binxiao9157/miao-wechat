===================================================================
Step 2: P0-2+P0-3 修复 Home.tsx 首屏空白 + setInterval 清理竞态
文件: src/pages/Home.tsx
问题: setCat 在 setTimeout(300ms) 内导致首屏 300ms 无猫咪；
      setInterval 在 setTimeout 回调内赋值，快速卸载时 ref 仍为 null 导致泄漏
===================================================================

--- a/src/pages/Home.tsx
+++ b/src/pages/Home.tsx
@@ -92,8 +92,13 @@

   useEffect(() => {
-    // 延迟执行重度逻辑，确保 UI 先渲染
+    // 同步初始化猫咪数据，避免首屏空白
+    const info = storage.getActiveCat();
+    setCat(info);
+    if (info) setVideoAspectRatio(null);
+
+    // 延迟执行积分等重度逻辑，确保 UI 先渲染
+    let intervalId: ReturnType<typeof setInterval> | null = null;
     const timer = setTimeout(() => {
-      const refreshCat = () => { ... };
-      refreshCat();
       const pointsInfo = storage.getPoints();
       ...

-      onlineTimerRef.current = setInterval(() => {
+      intervalId = setInterval(() => {
         ...
       }, 60000);
+      onlineTimerRef.current = intervalId;
     }, 300);

     return () => {
       clearTimeout(timer);
+      if (intervalId) clearInterval(intervalId);
       if (onlineTimerRef.current) clearInterval(onlineTimerRef.current);
       ...
     };
