===================================================================
Fix #7: 删除 Home.tsx 中被注释掉的 JSX 代码块
问题：大段被注释的 JSX（隐藏式功能按钮）留在正式代码中
     增加阅读噪音，不利于维护。
方案：直接删除注释块（10 行）。
===================================================================

--- a/src/pages/Home.tsx
+++ b/src/pages/Home.tsx
@@ -569,16 +569,6 @@
       />

-      {/* 隐藏式功能按钮 - 已根据需求彻底移除常驻入口，仅通过上方秘密手势唤起确认弹窗 */}
-      {/* 如果需要在开发模式下强制显示，可以取消下方注释 */}
-      {/*
-      <AnimatePresence>
-        {(showControls && isDev) && (
-          <motion.div ... />
-        )}
-      </AnimatePresence>
-      */}
-
       {/* 统一对话气泡 - 仿对话框样式与左侧滑入动画 */}
