diff --git a/src/pages/Login.tsx b/src/pages/Login.tsx
index 5e26bde..0ffc076 100644
--- a/src/pages/Login.tsx
+++ b/src/pages/Login.tsx
@@ -59,10 +59,10 @@ export default function Login() {
     const result = await sendCode(phone);
     if (result.success) {
       setCountdown(60);
-      // DEV mode feedback for verification code
-      const displayCode = result.mockCode || "888888";
-      alert(`测试环境验证码已发送: ${displayCode}\n(万能码: 888888)`);
-      setCode(displayCode); // Auto-fill for convenience
+      // 模拟模式下自动填充验证码（后端 SMS_ENABLED=false 时返回 mockCode）
+      if (result.mockCode) {
+        setCode(result.mockCode);
+      }
     } else {
       setError(result.error || "获取验证码失败");
     }
diff --git a/src/pages/Register.tsx b/src/pages/Register.tsx
index 62938ec..4b31552 100644
--- a/src/pages/Register.tsx
+++ b/src/pages/Register.tsx
@@ -42,10 +42,9 @@ export default function Register() {
     const result = await sendCode(phone);
     if (result.success) {
       setCountdown(60);
-      // DEV mode feedback for verification code
-      const displayCode = result.mockCode || "888888";
-      alert(`测试环境验证码已发送: ${displayCode}\n(万能码: 888888)`);
-      setCode(displayCode); // Auto-fill for convenience
+      if (result.mockCode) {
+        setCode(result.mockCode);
+      }
     } else {
       setError(result.error || "获取验证码失败");
     }
diff --git a/src/pages/ResetPassword.tsx b/src/pages/ResetPassword.tsx
index c9e2de4..fff27f7 100644
--- a/src/pages/ResetPassword.tsx
+++ b/src/pages/ResetPassword.tsx
@@ -37,10 +37,9 @@ export default function ResetPassword() {
     const result = await sendCode(phone);
     if (result.success) {
       setCountdown(60);
-      // DEV mode feedback for verification code
-      const displayCode = result.mockCode || "888888";
-      alert(`测试环境验证码已发送: ${displayCode}\n(万能码: 888888)`);
-      setCode(displayCode); // Auto-fill for convenience
+      if (result.mockCode) {
+        setCode(result.mockCode);
+      }
     } else {
       setError(result.error || "获取验证码失败");
     }
