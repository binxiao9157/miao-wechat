===================================================================
Step 6: P1-8+P1-9+P1-10 修复 MainLayout/ErrorBoundary/CSS
文件: src/components/layout/MainLayout.tsx, src/components/ErrorBoundary.tsx, src/index.css
问题: scale(0.98)+zIndex:-10 布局异常；ErrorBoundary 两按钮都硬刷新；
      全局 user-select:none 影响可访问性
===================================================================

--- a/src/components/layout/MainLayout.tsx
+++ b/src/components/layout/MainLayout.tsx
@@ -47,3 +47,2 @@
         animate={{
           opacity: isActive ? 1 : 0,
-          zIndex: isActive ? 10 : -10,
-          scale: isActive ? 1 : 0.98
+          zIndex: isActive ? 10 : 0
         }}

@@ -77,3 +76,2 @@
         animate={{
           opacity: isHome ? 1 : 0,
-          zIndex: isHome ? 0 : -10,
-          scale: isHome ? 1 : 0.98
+          zIndex: isHome ? 0 : 0
         }}

--- a/src/components/ErrorBoundary.tsx
+++ b/src/components/ErrorBoundary.tsx
@@ -30,5 +30,6 @@
-  private handleReset = () => {
-    this.setState({ hasError: false, error: null });
-    window.location.href = "/";
-  };
-  private handleReload = () => {
-    window.location.reload();
+  private handleReset = () => {
+    // 先尝试就地恢复，不刷新页面
+    this.setState({ hasError: false, error: null });
+  };
+  private handleGoHome = () => {
+    this.setState({ hasError: false, error: null });
+    window.location.href = "/";
   };

@@ (按钮) "重试一下" onClick 改为 this.handleReset
@@ (按钮) "返回首页" onClick 改为 this.handleGoHome

--- a/src/index.css
+++ b/src/index.css
@@ -48,8 +48,9 @@
-    /* 彻底禁用长按选词功能 */
-    -webkit-user-select: none;
-    -moz-user-select: none;
-    -ms-user-select: none;
-    user-select: none;
-    -webkit-touch-callout: none;
-  }
-  input, textarea {
-    -webkit-user-select: auto;
-    user-select: auto;
+    /* 禁用 iOS 长按弹出菜单，但保留文本可选性 */
+    -webkit-touch-callout: none;
+  }
+  /* 仅在按钮和交互元素上禁用选中 */
+  button, a, [role="button"] {
+    -webkit-user-select: none;
+    user-select: none;
+  }
+  input, textarea, select {
+    -webkit-user-select: auto;
+    user-select: auto;
   }
